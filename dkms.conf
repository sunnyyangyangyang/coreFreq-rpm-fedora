# === SIMPLIFIED DKMS.CONF (Remove custom signing, use DKMS default) ===

PACKAGE_NAME="corefreq"
PACKAGE_VERSION="@RPM_VERSION@"
BUILT_MODULE_NAME[0]="corefreqk"
BUILT_MODULE_LOCATION[0]="build" 
DEST_MODULE_LOCATION[0]="/extra"
MAKE[0]="make clean && make -C . all"
AUTOINSTALL="yes"

# REMOVED: POST_BUILD custom signing
# Let DKMS handle signing with its default system